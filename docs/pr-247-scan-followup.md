# PR 247 follow-up note

This branch exists as a fresh replacement PR for the earlier merged scanner work.
It is intentionally created from a clean main branch so the repository can open a new PR number instead of reusing the old merged one.
