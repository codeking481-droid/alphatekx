---
description: Run predictive healing scan — detect 8 failure patterns, auto-fix Tier 1, flag Tier 2 with report
agent: predictive-healer
---

Target: $ARGUMENTS

Run the Predictive Healer full scan on target $ARGUMENTS: load codebase → scan ALL 8 patterns (missing error handlers, API fallbacks, mobile breakpoints, security headers, perf optimizations, validation, a11y labels, SEO tags) → flag every potential issue → auto-fix Tier 1 (handlers, validation, a11y) → flag Tier 2 (security, perf) for user review → generate Predictive Healing Report with separate BROKEN vs PROACTIVE sections.

Begin predictive scan now.
