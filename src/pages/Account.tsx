import { LogOut, ShieldCheck } from 'lucide-react'
import { useAuth } from '../lib/auth'

export default function Account(){const{user,profile,signOut}=useAuth();return <div className="min-h-screen px-5 py-8 md:px-10"><div className="mx-auto max-w-3xl"><h1 className="text-xl font-semibold md:text-2xl">Account</h1><p className="mt-2 text-sm text-white/55">Manage your AlphaTekx profile.</p><div className="mt-6 rounded-xl border border-white/[.12] liquid-glass p-6 shadow-sm"><div className="flex items-center gap-3"><span className="grid size-11 place-items-center rounded-full bg-white/[.08]"><ShieldCheck size={19}/></span><div><p className="font-semibold">{user?.email}</p><p className="text-sm text-white/55">Signed in securely</p></div></div><dl className="mt-6 grid gap-3 sm:grid-cols-3"><Stat label="Credits" value={String(profile?.credits??0)}/><Stat label="Plan" value={profile?.plan??'free'}/><Stat label="Revenue" value={`NGN ${Number(profile?.revenue??0).toLocaleString()}`}/></dl><button onClick={()=>void signOut()} className="mt-6 flex min-h-11 items-center gap-2 rounded-lg border border-white/[.15] px-4 text-sm hover:border-[#E56B2D]"><LogOut size={16}/>Sign out</button></div></div></div>}
function Stat({label,value}:{label:string;value:string}){return <div className="rounded-xl border border-white/[.12] bg-white/[.04] p-4"><dt className="text-xs text-white/55">{label}</dt><dd className="mt-2 text-xl font-semibold capitalize">{value}</dd></div>}
